// @ts-nocheck

import "./Header.css";

export default function Header(){
    return(
        <header className="flex">
            <h1 className="flex mainTitle">HRnet</h1>
        </header>
    );
}